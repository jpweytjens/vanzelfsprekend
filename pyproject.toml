[project]
name = "klaarte"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Johannes Weytjens", email = "github@peregrinus.be" }
]
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.9",
    "mizani>=0.14",
    "numpy>=2.2.6",
]

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]

[tool.ruff]
extend-exclude = ["archive"]
