[[repos]]
repo = "https://github.com/pre-commit/pre-commit-hooks"
rev = "v6.0.0"
hooks = [
  { id = "trailing-whitespace" },
  { id = "end-of-file-fixer" },
  { id = "check-yaml" },
  { id = "check-merge-conflict" },
  { id = "check-added-large-files" },
]

[[repos]]
repo = "https://github.com/astral-sh/ruff-pre-commit"
rev = "v0.16.4"
hooks = [
  { id = "ruff", args = ["--fix"] },
  { id = "ruff-format" },
]

[[repos]]
repo = "https://github.com/astral-sh/uv-pre-commit"
rev = "0.12.5"
hooks = [{ id = "uv-lock" }]

[[repos]]
repo = "https://github.com/gitleaks/gitleaks"
rev = "v8.30.1"
hooks = [{ id = "gitleaks" }]

[[repos]]
repo = "https://github.com/crate-ci/committed"
rev = "v1.1.11"
hooks = [{ id = "committed", stages = ["commit-msg"] }]

[[repos]]
repo = "local"
hooks = [
  { id = "ty-check", name = "ty check src", language = "system", entry = "uv run ty check src", pass_filenames = false, files = "^src/" },
  { id = "reference-dir-guard", name = "docs/superpowers/ stays uncommitted", language = "system", entry = "bash -c 'changed=$(git diff --cached --name-only -- docs/superpowers/); if [ -n \"$changed\" ]; then printf \"%s\\n\" \"$changed\" >&2; echo \"docs/superpowers/ is local-only\" >&2; exit 1; fi'", pass_filenames = false, always_run = true },
]
