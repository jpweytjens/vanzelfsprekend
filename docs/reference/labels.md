# Labels

The functions that place text on a figure: `xlabel` and `ylabel` sit at the spine ends, `line_labels` and `label` beside the data instead of in a legend.

::: vanzelfsprekend.xlabel

::: vanzelfsprekend.ylabel

::: vanzelfsprekend.line_labels

::: vanzelfsprekend.label
